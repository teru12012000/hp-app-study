import { style } from "@vanilla-extract/css"

const styles = {
    container: style({
        width: "100vw",
        textAlign: "center",
    }),
    title: style({
        marginTop: 20,
    }),
}

export default styles
